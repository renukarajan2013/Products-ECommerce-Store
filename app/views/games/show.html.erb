<%- model_class_game = Game -%>
<%- model_class_product  = Product -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class_game.model_name.human.titleize %></h1>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class_game.human_attribute_name(:id) %></th>
      <th><%= model_class_product.human_attribute_name(:name) %></th>
      <th><%= model_class_game.human_attribute_name(:rating) %></th>
      <th><%= model_class_product.human_attribute_name(:price) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
     <% @game = Game.where(product_id: params[:product_id]).first %>
    <% @product = Product.where(id: @game.product_id ).first%>
    <tr>
        <td><%= @game.id%></td>
        <td><%= @product.name %></td>
        <td><%= @game.rating%></td>
        <td><%= @product.price %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_game_path(@game), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      game_path(@game),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
  </tbody>
</table>

